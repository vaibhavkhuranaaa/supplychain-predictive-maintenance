# Supply Chain Predictive Maintenance agent contract

Read `docs/STATE.md`, `docs/HANDOFF.md`, and fresh `graphify-out/` context before broad exploration. Inspect source directly when the graph does not cover the relevant files.

Use only public or properly licensed telemetry data. Never claim production reliability, maintenance savings, model performance, or deployment without versioned evidence. Evaluation must separate units, cover operating regimes, compare a baseline, and connect error to maintenance decisions.

Preserve unrelated dirty work. Use purpose branches, conventional commits, and the configured human identity only; never add AI/model author or co-author attribution. Deployment, publication, merging, public visibility, and paid resources are separate owner-gated actions.

The project remains `building`; do not advance it until ingestion, evaluation, reproducibility, disclosure, and a safe monitoring demo are verified. Delegation is optional and must be bounded.
